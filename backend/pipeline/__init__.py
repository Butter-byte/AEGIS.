"""Recovery pipeline / orchestration (Vikash) — the only module that wires the
observe -> diagnose -> plan -> twin -> safety -> execute flow.

Import from the submodules directly: `backend.pipeline.orchestrator`,
`backend.pipeline.ports`.
"""
